#ifndef AREA_H
#define AREA_H

#include <iostream>

float AreaTriangulo (float base, float altura);
/**
 * @brief Calcula e retorna a área do triângulo
 */

float AreaRetangulo (float base, float altura);
/**
 * @brief Calcula e retorna a área do retângulo
 */

float AreaQuadrado (float lado);
/**
 * @brief Calcula e retorna a área do quadrado
 */

float AreaCirculo (float raio);
/**
 * @brief Calcula e retorna a área do círculo
 */

float AreaPiramide (float base, float altura);
/**
 * @brief Calcula e retorna a área da pirâmide
 */

float AreaCubo (float lado);
/**
 * @brief Calcula e retorna a área do cubo
 */

float AreaParalelepipedo (float base, float altura, float largura);
/**
 * @brief Calcula e retorna a área do paralelepípedo
 */

float AreaEsfera (float raio);
/**
 * @brief Calcula e retorna a área do esfera
 */

#endif